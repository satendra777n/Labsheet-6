my_list = [1, 2, 2, 3, 4, 4, 5]
distinct_count = len(set(my_list)) # converting list to set to find distinct elements and getting its length

print("Number of distinct elements:", distinct_count) # displaying the count of distinct elements
