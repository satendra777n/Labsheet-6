my_list = [1, 2, 2, 3, 4, 4, 5]
unique_list = list(set(my_list)) # converting list to set and back to list to remove duplicates

print("List after removing duplicates:", unique_list) # displaying the list after removing duplicates
